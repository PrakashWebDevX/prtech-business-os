"""
Form Fill Agent — not yet implemented.

Stubbed so the supervisor graph can route to it without crashing; wire this
up after the Lead-Gen vertical slice (Step 1-3 in the build plan) is
working end-to-end. See PRTECH build prompt, Step 3, for the intended
input/output contract for this agent.
"""


async def run_form_fill(*args, **kwargs) -> dict:
    raise NotImplementedError(
        "The form_fill agent has not been built yet. "
        "See the PRTECH Business OS build plan, Step 3, for its spec."
    )
